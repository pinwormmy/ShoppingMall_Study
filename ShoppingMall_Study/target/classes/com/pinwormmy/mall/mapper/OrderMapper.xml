<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="com.pinwormmy.mall.mapper.OrderMapper">

	<resultMap type="com.pinwormmy.mall.dto.ProductDTO" id="produtDTO">
		<result column="produtNum" property="produtNum"/>
		<result column="productName" property="productName"/>
		<result column="productPrice" property="productPrice"/>
		<result column="thumbnailPath" property="thumbnailPath"/>		
	</resultMap>

	<resultMap type="com.pinwormmy.mall.dto.CartDTO" id="cartDTO">
		<result column="CartNum" property="CartNum"/>
		<result column="ProductNum" property="ProductNum"/>
		<result column="Quantity" property="Quantity"/>
		<result column="userId" property="userId"/>
		<collection property="productDTO" resultMap="productDTO"/>
	</resultMap>

	<select id="showCart" parameterType="cartDTO" resultMap="cartDTO">
		select a.cartNum, a.productNum, a.userId, a.quantity, b.productName, b.productPrice, b.thumbnailpath
			from cart a, productList b
			where a.productNum=b.productNum
            and userId=#{userId}
			order by cartNum asc;
	</select>	
	
	<insert id="addCart">
		insert into cart values(
			CARTNUM_SEQ.NEXTVAL, 
			#{productNum}, 
			#{quantity},			
			#{userId}
			)
	</insert>
	
	<delete id="deleteCart">
		DELETE FROM CART
    		WHERE cartNum=#{cartNum}
	</delete>
  
</mapper>